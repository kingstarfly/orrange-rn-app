import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  scrollViewContainer: {
    marginBottom: 20,
    marginTop: -30, // Should match the margin between sections
  },
});
